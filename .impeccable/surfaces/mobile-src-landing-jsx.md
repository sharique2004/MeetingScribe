---
version: 1
slug: "mobile-src-landing-jsx"
primary_target: "mobile/src/Landing.jsx"
related_targets: []
---

Scope: mobile/src/Landing.jsx (signed-out desktop landing; phone variant shows viewer entry). Mode: Persuade.
Audience: developers and technical Mac users deciding whether to run a shell command from a stranger's site; they arrive from the portfolio or GitHub.
Job/action: understand the local-first difference in seconds, then run the install one-liner or download the DMG.
Proof on hand: real install transcript behavior (curl, no Gatekeeper prompts), in-code recreation of the app's converged two-pane UI fed by ui-demos/data.js demo meeting (label as demo), approved claims: 45-min call in ~90s on Neural Engine; free/MIT/open source; nothing leaves the Mac (stated once, subtly; hero capsule banned).
Constraints: user-pinned visual world = the shipped app's converged aurora system (night #0a0a12, glass hairline panels, restrained aurora wash, aqua #5eead4/#7dd3fc accent, conic --angle border-beam as the one signature, Bricolage/Hanken/JetBrains type, DESIGN_BRIEF easing/motion rules). Zero em dashes in copy. No icon-card grids, no capsule pills, no fake liveness. Keep onOpenApp/phone contract, /install.sh serving, InsForge deploy pipeline.
Direction (seed 06e89c7b, grounded candidate 4, stagings declined): TERMINAL-FIRST. First viewport = dual-claim display headline over a monumental beam-traced terminal that types the real install command and prints a truthful install transcript; the app window "opens" below it as the scroll payoff (converged two-pane recreation, live-typing captions, aqua active-line halo); proof rows are micro-artifacts, not cards; install section keeps the accurate macOS 26 walkthrough.
Memorable moment: the page runs the same command it asks you to run, and what it installs opens right under it.
Unresolved: none blocking; real app screenshots may replace the in-code mock later if the user provides safe captures.
