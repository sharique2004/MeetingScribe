venv/
recordings/
practice/
models/

# INTERIM: engineering docs currently quote real meeting titles and participant
# names in their measurement tables. This repo is public. Ignored until the
# names are replaced with stable pseudonyms, then this line should be removed
# so the technical content is version-controlled again.
docs/

# Diarization eval fixtures derived from real recordings: meeting titles carry
# participant names and analysis.npz holds ECAPA voice embeddings (biometric
# speaker identifiers) for people who never agreed to have them published.
# This repo is public — keep them local. Synthetic labelled fixtures are what
# belongs in git; see docs/COUNT_ESTIMATION_DESIGN.md.
test/fixtures/
__pycache__/
*.pyc
.DS_Store
config.json
nohup.out
*.log
.claude/
templates/index-previous.html
.insforge/
mobile/.env*
mobile/node_modules/
mobile/dist/
macapp/.build/

# InsForge & AI agent skills
.insforge
.agent
.agents
.augment
.claude
.cline
.github/copilot*
.kilocode
.qoder
.qwen
.roo
.trae
.windsurf
dist/

# SPM build products for the neural-diarizer helper (vendored checkout +
# object files; the sources and Package.swift ARE tracked)
native/fluiddiarizer/.build/
native/fluiddiarizer/Package.resolved

# symlink form (recordings/ only matches the old directory)
recordings

# Engine state that follows MEETINGSCRIBE_DATA. In the packaged app that is
# ~/.meetingscribe; from a source checkout it is this folder, same as
# recordings/ and models/ already are.
/bin/
/sync_queue.json
/tap_state.json
/note-drafts/
